<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="App2.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    <ContentPage.Content>
        <StackLayout Padding="10,0,10,0" VerticalOptions="Center">
            <Label Text="Login" HorizontalTextAlignment="Center" FontSize="Medium" />
            <Entry Text="{Binding LoginValue, Mode=TwoWay}" FontSize="Medium" HorizontalTextAlignment="Center"/>
            <Label Text="Password" HorizontalTextAlignment="Center" FontSize="Medium" />
            <Entry Text="{Binding PasswordValue, Mode=TwoWay}" FontSize="Medium" HorizontalTextAlignment="Center"/>
            <Button VerticalOptions="Center" Text="Login" Command="{Binding LoginCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>